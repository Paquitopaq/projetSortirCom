<div class="custom-nav">
    <div class="nav-container">
        <a href="{{ path('app_home') }}" class="nav-btn">Accueil</a>
        <a href="{{ path('app_sortie') }}" class="nav-btn">Sorties</a>
        <a href="{{ path('app_login') }}" class="nav-btn">Se connecter</a>
        <a href="{{ path('app_register') }}" class="nav-btn">S’inscrire</a>

        <svg
                class="nav-outline"
                overflow="visible"
                width="650"
                height="60"
                viewBox="0 0 650 60"
                xmlns="http://www.w3.org/2000/svg"
        >
            <rect
                    class="nav-rect"
                    pathLength="100"
                    x="0"
                    y="0"
                    width="650"
                    height="60"
                    fill="transparent"
                    stroke-width="5"
            ></rect>
        </svg>
    </div>
</div>
