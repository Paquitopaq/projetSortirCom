{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/sortie-details.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <!-- En-tête -->
        <div class="detail-header">
            <a href="{{ path('app_sortie') }}" class="btn-back">
                <i class="fas fa-arrow-left"></i> Retour aux sorties
            </a>
            {% if app.user and (app.user == sortie.organisateur or is_granted('ROLE_ADMIN')) %}
                <div class="menu-dots">
                    <button class="dots-button">
                        <i class="fas fa-ellipsis-v"></i>
                    </button>
                    <div class="dropdown-menu" id="dropdownMenu">
                        <a href="{{ path('sortie_edit', {'id': sortie.id}) }}">
                            <i class="fas fa-edit"></i> Modifier
                        </a>
                        {% if sortie.etat != constant('App\\Enum\\Etat::CANCELLED') %}
                            <a href="{{ path('sortie_delete', {'id': sortie.id}) }}">
                                <i class="fas fa-times-circle"></i> Annuler la sortie
                            </a>
                        {% endif %}
                    </div>
                </div>
            {% endif %}
        </div>

        <!-- Grille principale : Image + Infos -->
        <div class="main-grid">
            <!-- Image -->
            <div class="image-card">
                <div class="detail-image">
                    {% if sortie.photoSortie %}
                        <img src="{{ asset('uploads/sorties/' ~ sortie.photoSortie) }}" alt="{{ sortie.nom }}" class="img-fluid">
                    {% endif %}

                    {% if sortie.lieu or sortie.site %}
                        <div class="location-badge">
                            <i class="fas fa-map-marker-alt"></i>
                            <span>
                                {% if sortie.lieu %}{{ sortie.lieu.nom }}{% endif %}
                                {% if sortie.site and sortie.lieu %} – {% endif %}
                                {% if sortie.site %}{{ sortie.site.nom }}{% endif %}
                            </span>
                        </div>
                    {% endif %}

                    {% if sortie.etat.value == 'Ouverte' %}
                        <span class="badge-now">Inscriptions ouvertes</span>
                    {% endif %}
                </div>
            </div>

            <!-- Infos principales -->
            <div class="info-card">
                <h1 class="detail-title">{{ sortie.nom }}</h1>

                {% if sortie.infoSortie %}
                    <div class="detail-description">
                        {{ sortie.infoSortie }}
                    </div>
                {% endif %}

                <div class="detail-grid">
                    <div class="detail-info-item">
                        <i class="fas fa-clock"></i>
                        <div>
                            <div class="info-label">Date et heure</div>
                            <div class="info-value">{{ sortie.dateHeureDebut|date('d/m/Y à H:i') }}</div>
                        </div>
                    </div>

                    <div class="detail-info-item">
                        <i class="fas fa-calendar-times"></i>
                        <div>
                            <div class="info-label">Limite d'inscription</div>
                            <div class="info-value">{{ sortie.dateLimiteInscription|date('d/m/Y') }}</div>
                        </div>
                    </div>

                    <div class="detail-info-item">
                        <i class="fas fa-hourglass-half"></i>
                        <div>
                            <div class="info-label">Durée</div>
                            <div class="info-value">{{ sortie.duree }} min</div>
                        </div>
                    </div>

                    <div class="detail-info-item">
                        <i class="fas fa-users"></i>
                        <div>
                            <div class="info-label">Participants</div>
                            <div class="info-value">{{ sortie.participants|length }}/{{ sortie.nbInscriptionMax }}</div>
                        </div>
                    </div>
                </div>

                <div class="action-buttons">
                    {% if app.user %}
                        {% set user = app.user %}

                        {% if user == sortie.organisateur %}
                            {% if sortie.etat.value == 'Créée' %}
                                <form method="post" action="{{ path('sortie_valider', {'id': sortie.id}) }}" style="flex: 1;">
                                    <button type="submit" class="btn-action btn-join" style="width: 100%;">
                                        <i class="fas fa-check-circle"></i> Publier la sortie
                                    </button>
                                </form>
                            {% else %}
                                <button class="btn-action btn-disabled" disabled>
                                    <i class="fas fa-ban"></i> Vous êtes l'organisateur
                                </button>
                            {% endif %}
                        {% elseif user in sortie.participants %}
                            <form method="post" action="{{ path('sortie_inscrire', {'id': sortie.id}) }}" style="flex: 1;">
                                <button type="submit" class="btn-action btn-unsubscribe" style="width: 100%;">
                                    <i class="fas fa-times"></i> Se désinscrire
                                </button>
                            </form>
                        {% elseif sortie.participants|length < sortie.nbInscriptionMax and sortie.etat.value == 'Ouverte' %}
                            <form method="post" action="{{ path('sortie_inscrire', {'id': sortie.id}) }}" style="flex: 1;">
                                <button type="submit" class="btn-action btn-join" style="width: 100%;">
                                    <i class="fas fa-check"></i> S'inscrire
                                </button>
                            </form>
                        {% else %}
                            <button class="btn-action btn-disabled" disabled>
                                <i class="fas fa-ban"></i> Inscriptions closes
                            </button>
                        {% endif %}
                    {% endif %}
                </div>
            </div>
        </div>

        <!-- Grille inférieure : Organisateur + Participants -->
        <div class="bottom-grid">
            <!-- Organisateur -->
            <div class="organizer-card">
                {% if sortie.organisateur %}
                    <a href="{{ path('app_profil_participant', {id: sortie.organisateur.id}) }}" class="organizer-link">
                        <div class="organizer-avatar">
                            {% if sortie.organisateur.getAvatarPath() %}
                                <img src="{{ asset(sortie.organisateur.getAvatarPath()) }}" alt="Avatar {{ sortie.organisateur.prenom }}">
                            {% else %}
                                {{ sortie.organisateur.prenom|first }}{{ sortie.organisateur.nom|first }}
                            {% endif %}
                        </div>
                        <div class="organizer-name">
                            {{ sortie.organisateur.prenom ~ ' ' ~ sortie.organisateur.nom }}
                        </div>
                        {% if sortie.organisateur.pseudo %}
                            <div class="organizer-pseudo">{{ sortie.organisateur.pseudo }}</div>
                        {% endif %}
                    </a>
                {% else %}
                    <div class="organizer-avatar">
                        <img src="{{ asset('assets/avatars/cchat.png') }}" alt="Avatar par défaut">
                    </div>
                    <div class="organizer-name">Organisateur inconnu</div>
                {% endif %}

                <div class="organizer-stats">
                    <div class="stat-item">
                        <div class="info-label">Organisé par</div>
                        <div class="info-value">
                            {% if sortie.organisateur %}
                                {{ sortie.organisateur.prenom }}
                            {% else %}
                                Inconnu
                            {% endif %}
                        </div>
                    </div>
                    <div class="stat-item">
                        <div class="info-label">Sorties créées</div>
                        <div class="info-value">
                            {% if sortie.organisateur and sortie.organisateur.sortiesOrganisees %}
                                {{ sortie.organisateur.sortiesOrganisees|length }}
                            {% else %}
                                0
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>

            <!-- Participants -->
            <div class="participants-card">
                <div class="participants-header">
                    <h2>Participants inscrits ({{ sortie.participants|length }})</h2>

                    {% if is_granted('ROLE_ADMIN') %}
                        <form method="post" action="{{ path('admin_add_participant', {id: sortie.id}) }}" style="margin-top: 10px;">
                            <div class="d-flex gap-2 align-items-center">
                                <select name="participantId" class="form-select" required style="max-width: 300px;">
                                    {% for participant in participants %}
                                        <option value="{{ participant.id }}">{{ participant.nom }} {{ participant.prenom }}</option>
                                    {% endfor %}
                                </select>
                                <button type="submit" class="btn btn-success">
                                    <i class="fas fa-user-plus"></i> Ajouter
                                </button>
                            </div>
                        </form>
                    {% endif %}
                </div>



                {% if sortie.participants|length > 0 %}
                    <div class="participants-grid">
                        {% for participant in sortie.participants %}
                            <div class="participant-wrapper">
                                <a href="{{ path('app_profil_participant', {id: participant.id}) }}" class="participant-card">
                                    <div class="participant-avatar">
                                        {{ participant.prenom|first }}{{ participant.nom|first }}
                                    </div>
                                    <div class="participant-name">
                                        {{ participant.pseudo ?? participant.prenom ~ ' ' ~ participant.nom }}
                                    </div>
                                </a>
                                {% if is_granted('ROLE_ADMIN') %}
                                    <div class="participant-admin-actions">
                                        <button class="btn-icon btn-make-organizer" onclick="openChangeOrganizerModal({{ participant.id }}, '{{ participant.prenom }} {{ participant.nom }}')" title="Définir comme organisateur">
                                            <i class="fas fa-crown"></i>
                                        </button>
                                        <form method="post" action="{{ path('admin_remove_participant', {id: sortie.id}) }}" style="display: inline;">
                                            <input type="hidden" name="participantId" value="{{ participant.id }}">
                                            <input type="hidden" name="motif" value="Retrait manuel par l'admin">
                                            <button type="submit" class="btn-icon btn-remove-participant"
                                                    onclick="return confirm('Confirmer le retrait de ce participant ?')"
                                                    title="Retirer de la sortie">
                                                <i class="fas fa-user-minus"></i>
                                            </button>

                                        </form>

                                    </div>
                                {% endif %}
                            </div>
                        {% endfor %}
                    </div>
                {% else %}
                    <p style="color: #888;">Aucun participant pour le moment.</p>
                {% endif %}

                {# Bouton CSV pour admin #}
                {% if is_granted('ROLE_ADMIN') %}
                    <div class="admin-buttons">
                        <button class="btn-admin-action" id="openCsvModal">
                            <i class="fas fa-file-csv"></i> Importer CSV
                        </button>
                    </div>
                {% endif %}

                {# Formulaire CSV caché pour être injecté dans le modal #}
                <div id="csvFormWrapper" style="display:none;">
                    {{ form_start(form, {'attr': {'id': 'csvForm', 'enctype': 'multipart/form-data'}}) }}
                    <div style="margin-bottom: 10px;">
                        {{ form_widget(form['csv_file']) }}
                    </div>
                    <button type="submit" class="btn-action btn-join">
                        <i class="fas fa-upload"></i> Importer le fichier
                    </button>
                    {{ form_end(form) }}
                </div>

            </div>
        </div>
    </div>

    {% include 'partials/_footer.html.twig' %}
{% endblock %}


{% block javascripts %}


    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        // Toggle menu trois points
        document.addEventListener('DOMContentLoaded', function() {
            const dotsButton = document.querySelector('.dots-button');
            const dropdownMenu = document.getElementById('dropdownMenu');

            if (dotsButton && dropdownMenu) {
                dotsButton.addEventListener('click', function(event) {
                    event.stopPropagation();
                    dropdownMenu.classList.toggle('show');
                });

                // Fermer le menu si on clique ailleurs
                document.addEventListener('click', function(event) {
                    if (!dropdownMenu.contains(event.target) && !dotsButton.contains(event.target)) {
                        dropdownMenu.classList.remove('show');
                    }
                });
            }
        });

        document.addEventListener('DOMContentLoaded', function() {
            const openCsvBtn = document.getElementById('openCsvModal');
            const csvFormWrapper = document.getElementById('csvFormWrapper');

            if (openCsvBtn && csvFormWrapper) {
                openCsvBtn.addEventListener('click', () => {
                    Swal.fire({
                        title: 'Importer un fichier CSV',
                        html: csvFormWrapper.innerHTML,
                        showConfirmButton: false,
                        showCloseButton: true,
                        didOpen: () => {
                            const modalForm = Swal.getPopup().querySelector('form');
                            if (modalForm) {
                                // Le formulaire est soumis normalement vers Symfony
                                modalForm.addEventListener('submit', (e) => {
                                    // Pas besoin de JS, submission normale
                                });
                            }
                        }
                    });
                });
            }

            // Messages flash avec SweetAlert2
            {% for label, messages in app.flashes %}
            {% for message in messages %}
            Swal.fire({
                icon: '{{ label == 'success' ? 'success' : (label == 'danger' or label == 'error' ? 'error' : 'info') }}',
                title: '{{ label == 'success' ? 'Succès' : (label == 'danger' or label == 'error' ? 'Erreur' : 'Information') }}',
                text: '{{ message|raw }}',
                confirmButtonColor: '#FF6B00',
                timer: 3000,
                timerProgressBar: true
            });
            {% endfor %}
            {% endfor %}
        });

    <script>

    </script>


{% endblock %}
